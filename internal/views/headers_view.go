package views

const (
	// The header used to get the event type.
	EventType = "event-type"
	// The header used to get the s3 prefix.
	S3Prefix = "s3-prefix"
	// The temporary object prefix
	TempObjectPrefix = "s3-temp-prefix"
)
